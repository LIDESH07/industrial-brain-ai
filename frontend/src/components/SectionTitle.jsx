function SectionTitle({ title }) {
  return (
    <h2
      style={{
        color: "white",
        textAlign: "center",
        fontSize: "40px",
        margin: "60px 0",
      }}
    >
      {title}
    </h2>
  );
}

export default SectionTitle;